{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Generate Payment Receipt PDF Parameters",
  "description": "Schema para validar parámetros de generación de recibo de pago específico",
  "type": "object",
  "properties": {
    "bookingId": {
      "type": "string",
      "description": "ID único de la reserva en Beds24",
      "minLength": 1,
      "pattern": "^[0-9]+$"
    },
    "distribucion": {
      "type": "string",
      "description": "Descripción opcional de la distribución de camas del apartamento",
      "maxLength": 500
    }
  },
  "required": ["bookingId"],
  "additionalProperties": false
}