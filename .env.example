# Bot WhatsApp TeAlquilamos - Variables de Entorno
# Copia este archivo a .env y completa con tus valores

# === WHAPI CLOUD ===
# Token de tu canal en Whapi Cloud
WHAPI_TOKEN=tu_token_aqui
# ID del canal (aparece en el dashboard de Whapi)
WHAPI_CHANNEL_ID=PUNISH-XXXXX
# URL base de la API (no cambiar)
WHAPI_API_URL=https://gate.whapi.cloud/

# === OPENAI ===
# API Key de OpenAI (empieza con sk-)
OPENAI_API_KEY=sk-...
# ID del Assistant configurado con las funciones
ASSISTANT_ID=asst_...

# === N8N WEBHOOKS ===
# URL base de tu instancia n8n
N8N_WEBHOOK_URL=https://tu-n8n.com/webhook/tealquilamos
# API Key de n8n (opcional, si configuraste autenticación)
N8N_API_KEY=

# === CONFIGURACIÓN DE LA APP ===
# Puerto del servidor (default: 3008)
PORT=3008
# Modo debug (true/false)
DEBUG_MODE=true
# Ruta del archivo de logs
DEBUG_LOG_PATH=./logs/debug.log

# === CONFIGURACIÓN DE MEMORIA ===
# Archivo donde se guardan los perfiles (mientras no uses DB)
GUEST_PROFILES_PATH=./data/guest_profiles.json

# === CONFIGURACIÓN DE NGROK (Desarrollo) ===
# Si usas ngrok pro, puedes configurar un subdominio fijo
NGROK_AUTHTOKEN=
NGROK_SUBDOMAIN=

# === FIREBASE (Futuro deployment) ===
# Para cuando migres a Firebase Functions
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# === MONITOREO (Opcional) ===
# Sentry para tracking de errores
SENTRY_DSN=
# Webhook para alertas críticas
ALERT_WEBHOOK_URL=


# === LÍMITES Y SEGURIDAD ===
# Máximo de mensajes por usuario por minuto
RATE_LIMIT_PER_USER=10
# Tiempo de expiración de pre-reservas (minutos)
BOOKING_EXPIRATION_MINUTES=30
# Lista de números de prueba (separados por coma)
TEST_NUMBERS=